# Core inference dependencies for the deployed Streamlit app
# (fastapi/uvicorn are NOT needed here -- the deployed container runs only
# Streamlit, loading the model directly, to keep memory usage low on
# free-tier hosting. app.py/FastAPI remains in the repo for local/API use.)

# CPU-only PyTorch index (avoids pulling unnecessary multi-GB GPU/CUDA
# packages -- Streamlit Cloud has no Dockerfile step to do this separately,
# so it's specified directly here instead)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1

streamlit==1.38.0
scikit-learn==1.5.2
joblib==1.4.2
pandas==2.2.3
numpy==1.26.4
requests==2.32.3
